workflows:
  ios-workflow:
    name: iOS Build
    environment:
      xcode: latest
    scripts:
      - name: Build app
        script: |
          xcodebuild -project sketch-elements.xcodeproj \
          -scheme sketch-elements \
          -sdk iphoneos \
          -configuration Release \
          build
    artifacts:
      - build/**/*.ipa
